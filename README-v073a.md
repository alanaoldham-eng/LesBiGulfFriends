# v07.3a messages Suspense fix

Fixes the Next.js build error:

- `useSearchParams() should be wrapped in a suspense boundary at page "/messages"`

Included:
- `app/messages/page.tsx`

Apply:
1. unzip into repo root
2. overwrite the file
3. run `npm run build`
