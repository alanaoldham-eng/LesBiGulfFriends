Events:
- This package overwrites your uploaded app/events-app/page.tsx with safe targeted changes if the upload was present.
- If your local events page differs from the uploaded one, compare before overwrite.

Groups:
- Use components/ReactionRoster.tsx for emoji hover rosters.
- Replace plain emoji buttons with the ReactionRoster component in your group thread page if needed.

Schema context used:
The app has `invites`, `event_media`, `event_media_comments`, `event_media_reactions`, `event_message_reactions`, `group_message_reactions`, and `messages` tables in the uploaded schema. fileciteturn101file0
