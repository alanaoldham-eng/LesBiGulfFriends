Your uploaded app/events-app/page.tsx is truncated mid-file, so I did not overwrite it blindly.

Do these edits in your current app/events-app/page.tsx:

1. Make sure these imports exist:
import { canEditPastEventMistake, deleteEventByOwner } from "../../lib/eventCrud";

2. Keep form collapsed by default:
const [formMode, setFormMode] = useState<"closed" | "create" | "edit">("closed");

3. Add Create Event CTA for users with 1+ karma:
{karmaPoints >= 1 && formMode === "closed" ? <button className="button" onClick={openCreate}>Create Event</button> : null}

4. Delete events using deleteEventByOwner(...) from lib/eventCrud.ts

5. For event videos use:
<video controls preload="metadata" style={{ width: "100%", borderRadius: 12, background: "#000" }}>
  <source src={item.media_url} type={item.media_type || "video/mp4"} />
</video>

6. For past events, only show Edit when:
canEditPastEventMistake(ev)

7. Before awarding a badge, check listBadgesForUser(userId) and stop if the user already has the same event badge.

Your missing lib/components/messages files were likely the real reason several buttons appeared to do nothing.
