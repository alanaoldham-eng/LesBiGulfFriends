# v070.2 events landing patch

Included:
- app/events-app/page.tsx

Adds:
1. Upcoming Events first, then Create Event, then Past Events
2. Selected event continues to support view/upload for event media
3. Award Event Badge uses member name dropdown instead of raw user ID
